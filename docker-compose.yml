version: "3"
services:
  frontend:
    image: yanggwangseong/docker-frontend
    volumes:
      - /app/node_modules
      - ./frontend:/app
    stdin_open: true
    mem_limit: 128m
  
  nginx:
    restart: always
    image: yanggwangseong/docker-nginx
    ports:
      - "80:80"
  
  backend:
    image: yanggwangseong/docker-backend
    container_name: app_backend
    volumes:
      - /app/node_modules
      - ./backend:/app
    mem_limit: 128m